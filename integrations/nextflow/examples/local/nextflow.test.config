includeConfig 'nextflow.config'

// CI/local module execution uses the just-built CLI. Production examples keep
// docker.enabled and the immutable image digest from nextflow.config.
docker.enabled = false
